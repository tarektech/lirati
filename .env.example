# Canonical site URL (no trailing slash). Required in production for correct
# canonicals, Open Graph URLs, sitemap, and JSON-LD. Match this to your
# Google Search Console property (www vs non-www).
#
# Example: https://lirati.example.com
NEXT_PUBLIC_SITE_URL=
